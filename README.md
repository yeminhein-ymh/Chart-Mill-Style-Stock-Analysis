# ChartMill Style Stocks Analysis

A Streamlit stock-analysis workspace inspired by ChartMill-style navigation and market-monitoring pages.

## Run

```powershell
streamlit run streamlit_app.py
```

The app uses `yfinance` when available and falls back to deterministic sample data if live data is unavailable.
